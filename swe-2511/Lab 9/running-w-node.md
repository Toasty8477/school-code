### Program Interpretation
The program generates three random integers then checks to see if any are less than zero or the result of val2 - val1 will be negtive
If any of the values are less than zero or val2 - val1 is negative it will generate the values again until all values are larger than zero and val2 - val1 is positive
Then the program does the quadriatic formula using the values as a, b, and c
Afterwards the program will output the equation it used, the answers, and how many attempts it took to generate the values for a, b, and c

### Output
```
1x^2 + 6x + 6
-1.2679491924311228 -4.732050807568877
3
```